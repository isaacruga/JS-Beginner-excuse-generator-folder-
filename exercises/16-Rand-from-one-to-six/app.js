function getRandomInt(a,b)
{
	var randomNumber = Math.floor(Math.random()*5 +1);
	return randomNumber;
}
console.log(getRandomInt());

// no me entero cuándo tengo que poner dentro los parámetros
// y cuando no ej. getRandomInt(a,b); console.log(getRandomInt(___));